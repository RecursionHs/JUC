<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ssm.demo.dao.PictureDao">
        <resultMap id="PictureResult" type="com.ssm.demo.entity.Picture">
            <result property="id" column="id"></result>
            <result property="path" column="path"></result>
            <result property="remark" column="remark"></result>
            <result property="createTime" column="create_time"></result>
        </resultMap>

    <insert id="save" parameterType="com.ssm.demo.entity.Picture">
          insert into tb_ssm_picture(path,remark)
          values (#{path},#{remark})
    </insert>

    <select id="findPictures" parameterType="Map" resultMap="PictureResult">
        select id,path,remark,create_time from tb_ssm_picture
        where is_deleted=0
        order by id desc
        <if test="start!=null and limit!=null">
            limit #{start},#{limit}
        </if>
    </select>

    <select id="getTotalPictures" parameterType="Map" resultType="int">
        select count(*) from tb_ssm_picture
        where is_deleted=0
    </select>

    <select id="findPictureById" parameterType="Integer" resultMap="PictureResult">
        select id,path,remark from tb_ssm_picture where is_deleted = 0 and id= #{id}
    </select>
</mapper>